## Iterate over all facts
#foreach($fact in $facts)
#fact($fact)

#end

## Iterate over all rules
#foreach($rule in $rules)
#predicates($rule.getHead().getPredicates()) ##
:- ##
#predicates($rule.getBody().getPredicates())##
.
#end

## Macro for a single fact
#macro(fact $f)
$f.getName()($f.getConstant().getName()).##
#end

## Macro for multiple predicates, separated by ','
#macro(predicates $ps)
#set($maxIndex = $ps.size() - 1)
#foreach($n in [0..$maxIndex])
#if($n != 0)
, ## Comma separates the predicates
#end
#predicate($ps.get($n))
#end
#end

## Macro for a single predicate
#macro(predicate $p)
$p.getName()($p.getVariable())##
#end